{
  "name": "talksys-desktop",
  "version": "1.1.0",
  "private": true,
  "main": "main.cjs",
  "scripts": {
    "start": "electron .",
    "check": "node --check main.cjs && node --check preload.cjs && node --check overlay-preload.cjs && node --check renderer.js && node --check overlay.js && node --check realtime-voice.js"
  },
  "devDependencies": {
    "electron": "44.0.0"
  }
}
